// 宏包相关的检查函数
import { IPackage } from "../typings/packages"

/**
 * 检查版本号更新
 */
// const shouldUpdate = (): boolean => {
//     const versionRegex = /v([0-9]+).([0-9]+).([0-9]+)/
//     return false
// }

export const checkPackageUpdate = () => {}

/**
 * 检查包
 * @param packName 包名
 */
export const checkPackageInfo = (pack: IPackage) => {}
